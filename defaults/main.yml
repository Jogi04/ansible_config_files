# base + server role
is_ssh_server: true
gateway: 10.0.0.1
netmask: 255.0.0.0
dns_server: 10.0.0.5

# workstation role
primary_gpu: nvidia
integrated_gpu: intel
bluetooth: true
laptop: true

# arch_linux_install role
install_drive: /dev/nvme0n1
root_partition_size: 100G

# proxmox_install role
