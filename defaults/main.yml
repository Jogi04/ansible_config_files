# base role
hostname:
is_internet_facing: [true, false]

# server role
instance_type: [nextcloud]

# workstation role
primary_gpu: [nvidia]
integrated_gpu: [intel]
bluetooth: [true, false]
laptop: [true, false]
