# base role
hostname:
is_ssh_server: [true, false]
is_dns_server: [true, false]
is_http_server: [true, false]
is_https_server: [true, false]
is_vpn_server: [true, false]

# server role
instance_type: [nextcloud]

# workstation role
primary_gpu: [nvidia]
integrated_gpu: [intel]
bluetooth: [true, false]
laptop: [true, false]
