- name: install packages
  package:
    name: "{{ item }}"
    state: latest
  loop:
    - ansible
    - bash-completion
    - curl
    - flatpak
    - git
    - nano
    - nfs-utils
    - nmap
    - openssh
    - openvpn
    - rsync
    - wget
    - vim
    - zsh

    #desktop apps
    - arduino
    - arduino-avr-core
    - firefox
    - keepassxc
    - pycharm-community-edition
    - torbrower-launcher
    - wireshark
    - vlc
