- name: install packages
  package:
    name: "{{ item }}"
    state: latest
  loop:
    # terminal utils
    - speedtest-cli
    - nmap
    # development utils
    - ansible
    - openscad
    - virtualbox
    - pycharm-community-edition
    - arduino
    - arduino-avr-core
    - kdevelop
    - wireshark
    # desktop utils
    - konsole
    - firefox
    - vlc
    - gwenview
    - spectacle
    - filelight
    - keepassxc
    - kate
    - partitionmanager
