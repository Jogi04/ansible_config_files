- name: install snap
  apt:
    name: snapd
    state: latest

- name: install nextcloud from snap
  snap:
    name: nextcloud
    state: present

- name: reboot server to apply nextcloud paths
  reboot:
